# Production Reproducibility Audit

## Current validated checkpoint

- Branch: `main`
- Validated production checkpoint: `b08b6316a4e182be5993f64fcb85e9a7606a2821`
- Environment: `C:\venvs\drosophila-book`
- Python: 3.12
- Production notebooks: `01` through `09`

The production workflow has undergone both a controlled dependency-isolation audit and a final author-side fresh-kernel verification. The nine production notebooks reproduce the accepted book-chapter results without runtime dependence on private absolute-area data, diagnostic notebooks, obsolete assignment files, or stale S1–S5 outputs.

## Production chain

Run the notebooks from the repository root in numerical order, using a fresh kernel for each notebook:

1. `01_Preprocessing.ipynb`
2. `02_DTW_Clustering.ipynb`
3. `03_Feature_Engineering.ipynb`
4. `04_Feature_Correlation_and_Selection.ipynb`
5. `05_PCA_and_Clustering.ipynb`
6. `06_NMF_and_Clustering.ipynb`
7. `07_Cluster_Agreement.ipynb`
8. `08_k2_Feature_Characterization.ipynb`
9. `09_k3_Strict_Signature_Feature_Analysis.ipynb`

The sole direct biological analysis input required by the production chain is:

`data/raw/Delta cell area.xlsx`

Absolute cell-area data are not required by the production workflow.

## Final fresh-kernel verification

The full `01` → `09` production sequence was rerun manually with a fresh kernel for every notebook after completion of the production-code and markdown harmonization work.

All nine notebooks completed without error.

A separate clean clone of the public repository at this checkpoint completed the full `01` → `09` production chain successfully using fresh-kernel execution and remained Git-clean afterward.

The verification reproduced the expected production interfaces, assignments, feature tables, statistical analyses, and book-chapter figures. No executable source changes were made during the verification.

One execution-state difference appeared in Notebook 04 after the run. Direct comparison with the committed notebook confirmed:

```text
Changed cell sources: []
```

The execution-only difference was restored, leaving the repository clean.

## Controlled dependency-isolation audit

A separate controlled audit tested the complete production chain with prohibited or non-production inputs made unavailable.

The audit physically isolated or removed access to:

- `data/raw/Cell Area.xlsx`
- `private_future_work/`
- diagnostic data and diagnostic figures
- stale absolute-area-derived interfaces
- obsolete S1–S5 production artifacts
- legacy strict-signature interfaces
- diagnostic notebook dependencies

All nine production notebooks executed successfully under those conditions.

The audit confirmed that the production workflow requires only the released delta-area input plus production outputs generated by preceding notebooks.

Quarantined provenance files were restored afterward to their original paths with matching SHA-256 hashes.

## Source integrity

Before the final markdown harmonization pass, all production notebook code-cell sources were fingerprinted in deterministic notebook order.

- Total ordered code cells: 97
- Aggregate SHA-256 fingerprint:

```text
c5fda1d9e918482e924c7cbd4493360a149fe42a91260a1c47582e39a22b00bb
```

After the markdown-only revision:

- all 97 ordered code-cell fingerprints matched;
- code-cell counts and positions were unchanged;
- outputs, execution counts, and notebook metadata were unchanged by the markdown pass;
- parsed differences were confined to markdown `source` fields;
- no CSV, figure, requirement, diagnostic artifact, or other repository file changed as part of that pass.

## Core dataset invariants

The validated production workflow retains the following dataset structure:

- source trajectories: 131 cells
- movie cell counts: 43 / 35 / 53
- movie sequence lengths: 39 / 41 / 48 observations
- sampling interval: 20 minutes
- retained analytical cells: 108
- cells with leading missingness: 31
- cells with trailing missingness: 58
- retained trajectories with internal gaps: 0

For the analyzed dataset, no internal-gap interpolation is invoked.

## Feature pipeline invariants

- generated feature columns: 56
- usable engineered features before correlation reduction: 55
- final reduced feature matrix: 108 × 45
- `sample_entropy` is excluded because of non-finite values
- `energy` is not part of the exported usable candidate matrix
- PCA production representation: 12 principal components
- NMF production representation: rank 5

## Primary k=2 result

The production analysis audits six clustering streams:

- DTW-Ward
- DTW-KMeans
- PCA-Ward
- PCA-KMeans
- NMF-Ward
- NMF-KMeans

The strict k=2 consensus uses the stability-qualified five-stream set:

- DTW-Ward
- DTW-KMeans
- PCA-KMeans
- NMF-Ward
- NMF-KMeans

PCA-Ward remains visible in the complete audit but is excluded from strict consensus because of dimensional sensitivity.

Validated k=2 consensus results:

- unanimous five-stream consensus: 88 / 108 cells
- at least four-of-five agreement: 98 / 108 cells
- majority assignment: 108 / 108 cells

The unanimous Stage 1 feature analysis and full-cohort Stage 2 majority analysis reproduce the accepted manuscript results and Figure 4.

## Secondary k=3 result

The complete k=3 audit also contains six clustering streams.

The stability-qualified strict five-stream set is:

- DTW-Ward
- DTW-KMeans
- PCA-KMeans
- NMF-Ward
- NMF-KMeans

PCA-Ward is retained in the complete six-stream audit but excluded from the strict set because its k=3 partition is sensitive to the number of retained principal components.

### DTW-KMeans k=3

Production DTW-KMeans uses scikit-learn KMeans on the unscaled DTW dissimilarity-profile rows with:

- `init="k-means++"`
- `n_init=50`
- `random_state=42`

Validated cluster sizes:

- 49
- 31
- 28

Agreement with DTW-Ward:

- RI = 0.763240
- ARI = 0.480327
- NMI = 0.618878

The dominant DTW-profile KMeans elbow remains at k=2.

### PCA-Ward dimensional sensitivity

Validated k=3 PCA-Ward cluster sizes:

- 12 PCs: 20 / 32 / 56
- 13 PCs: 47 / 48 / 13
- 14 PCs: 13 / 11 / 84

Between 22 and 48 matched memberships change across these nearby dimensions. PCA-Ward therefore remains a valid audited stream but is not included in the stability-qualified strict k=3 set.

### Exact-signature recurrence

The five-stream k=3 signature analysis yields:

- 31 total exact signatures
- four strong recurrent signatures: 54 cells
- eight-signature broader recurrent tier: 81 cells
- 19 singleton signatures

Signature-frequency pattern:

```text
16, 14, 13, 11
8, 7, 7, 5
2, 2, 2, 2
19 singletons
```

The four strong recurrent core signatures therefore contain:

```text
16 / 14 / 13 / 11 = 54 cells
```

The former five-group S1–S5 / 57-cell result is obsolete and is not part of the current production analysis.

## k=3 feature characterization

For the four-signature strong recurrent core:

- BH-significant global tests: 32 / 45
- BY-significant global tests: 25 / 45
- all 32 BH-significant features have at least one Holm-adjusted significant pairwise contrast
- maximum rank eta-squared is approximately 0.834

For the broader eight-signature recurrent tier:

- BH-significant global tests: 33 / 45
- BY-significant global tests: 28 / 45
- 31 of the 33 BH-significant features have at least one Holm-adjusted significant pairwise contrast
- maximum rank eta-squared is approximately 0.916

Strong-core versus broader-tier feature rankings are highly stable:

- Spearman rank correlation ≈ 0.973
- Pearson effect-size correlation ≈ 0.980
- top-10 overlap: 10 / 10
- top-15 overlap: 14 / 15

This comparison evaluates robustness to tier inclusion; it is not an independent replication.

## Book-chapter figures

The final production figures were validated during the controlled audit.

For Book Chapter Figures 2–5:

- PNG and TIFF outputs matched the accepted files
- decoded image pixels matched
- dimensions matched
- 600 dpi metadata was confirmed
- TIFF LZW compression was confirmed

Figure 5 represents the four-signature strong recurrent core and contains six accepted feature panels:

1. standard deviation
2. sharpness index
3. initial fall amplitude
4. slope of oscillation magnitude
5. high-frequency band power
6. average time between peaks

Figure 5 uses the 54-cell core with signature sizes 16 / 14 / 13 / 11.

## Diagnostic and provenance materials

Diagnostic notebooks and files were used during revalidation to establish the accepted production design.

They are not runtime dependencies of the production notebook chain.

Examples include:

- `02_DTW_KMeans_DBSCAN_Diagnostic.ipynb`
- notebooks named `*_DIAGNOSTIC_ONLY.ipynb`
- `data/diagnostic/`
- `figures/diagnostic/`

The private working repository may retain these materials as scientific and computational provenance. Their presence in the private repository does not imply that they must be included in the final public release snapshot.

## Legacy and private artifacts

The following categories are not required by the active production workflow:

- legacy `3_cluster_assignments_*.csv` files
- obsolete S1–S5 / 57-cell outputs
- obsolete strict-signature interfaces superseded by the current stability-qualified k=3 outputs
- absolute-area-derived processed files
- `data/raw/Cell Area.xlsx`
- `private_future_work/`

Searches of the tracked repository confirmed that remaining consumers of these artifacts are limited to diagnostic notebooks, private future-work material, historical audit text, or explicit documentation that the legacy files are not regenerated.

The final public repository should therefore be created from a clean validated snapshot rather than by exposing the history of the private working repository.

## Requirements

The validated environment is pinned in `requirements.txt`, including:

- NumPy 2.1.3
- pandas 2.2.3
- SciPy 1.15.3
- scikit-learn 1.6.1
- Matplotlib 3.10.0
- statsmodels 0.14.4
- scikit-posthocs 0.11.4
- tslearn 0.6.4
- Pillow 12.3.0

## Audit conclusion

The nine-notebook production workflow is reproducible from the delta-area input alone, reproduces the accepted k=2 and k=3 results, and has no runtime dependency on private absolute-area data, diagnostic notebooks, or obsolete analytical artifacts.

The current production notebooks have passed both controlled dependency-isolation validation and final author-side fresh-kernel execution.

The public reproducibility release has been completed and archived as version 1.0.0 with DOI 10.5281/zenodo.22217053.
